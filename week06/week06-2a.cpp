/// week06-2a.cpp CPE 第2題 UVA 10107 - what is the Median?
#include <iostream>
#include <vector>
using namespace std;
int main()
{
    vector<int> a;
    int now;
    while( cin >> now ){
        a.push_back(now);
        cout << now << endl;
    } /// 在小黑裡輸入時，結束全部輸入 Ctrl-z 加 Enter
}
